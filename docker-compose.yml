version: '2.0'
services:
    backend:
        container_name: backend
        build: .
        image: backend
        volumes:
            - /home/nippit/Back-End/public:/tmp
        ports:
            - "3000:3000"
        restart: always